name: execute tests
run-name: tests execution
on:
  push:
    branches: [master]
jobs:
  echo_header:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Header
        run: echo "Running Tests"
